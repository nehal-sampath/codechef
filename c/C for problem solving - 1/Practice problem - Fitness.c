// Complete the Code

#include <stdio.h>

int main() {
    int t,X;
    int i = 1;
    scanf("%d", &t );
    while (i <= t){
        scanf("%d", &X);
        printf("%d\n", (2*X)*5);
        i = i+1;
    }
    return 0;
}
 
